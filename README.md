
  # Visualize Migration Flow Map

  This is a code bundle for Visualize Migration Flow Map. The original project is available at [38671635] https://otho-dashboard.figma.site/

  ## Running the code

  Run `npm i` to install the dependencies.

  Run `npm run dev` to start the development server.
  
